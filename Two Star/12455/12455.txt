題目要求我們判斷是否可以從一堆給定長度的bar中，拼湊成特定長度的bar
=====
先將所有的bar長度存入陣列

並在儲存的同時，將超出特定長度的bar直接刪除

接著我們使用一個bool陣列來表示每次取樣的bar，每取過一次後+1

true(1)表示取
false(0)表示不取

「取樣陣列長度」與「bar長度陣列」的size相同

假設它們的長度是5

那取樣陣列會從
00000
00001
00010
.
.
.取到 11111

並在每次取樣時，判斷所取bar總和是否與要求的特定長度相同